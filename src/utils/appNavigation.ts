import { NavigatorScreenParams } from '@react-navigation/native';
import { ImageURISource } from 'react-native';

export interface TabNavigatorParamsList {
  Login: string;
  ForgotPassword: string,
  VerifyOTP: string,
  Setting: string,
  Profile: string,
  Home: string,
  mainApp: string,
  HomePage: string,
  HomeTab: string
}